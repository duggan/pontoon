sudo: false # use Travis container based infrastructure

language: python

env:
  global:
    - MOCK=1
  matrix:
  - TOXENV=py26,lib,coverage
  - TOXENV=py27,lib,coverage,coveralls
  - TOXENV=py33,lib,coverage
  - TOXENV=py34,lib,coverage
  - TOXENV=pypy,lib,coverage
  - TOXENV=pypy3,lib,coverage
  - TOXENV=py26,install
  - TOXENV=py27,install
  - TOXENV=py33,install
  - TOXENV=py34,install
  - TOXENV=pypy,install
  - TOXENV=pypy3,install

install:
  - pip install tox

before_script:
  - git clone https://github.com/sstephenson/bats.git /tmp/bats
  - mkdir -p /tmp/local
  - bash /tmp/bats/install.sh /tmp/local
  - export PATH=$PATH:/tmp/local/bin

script:
  - tox

notifications:
  email:
    on_success: never
